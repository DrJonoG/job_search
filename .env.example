# Job Search Tool - Environment Configuration
# Copy this file to .env and fill in your API keys.
#
# Sources needing keys: Adzuna, Reed, USAJobs, Jooble, SerpAPI (Google Jobs), Findwork, CareerJet.
# Optional: GREENHOUSE_BOARD_TOKENS. JobSpy & LinkedIn use scraping (pip install python-jobspy); no keys.

# ══════════════════════════════════════════════════════════════
# FREE API Keys (register at each site – all are free tier)
# ══════════════════════════════════════════════════════════════

# Adzuna – international job search
# Register: https://developer.adzuna.com/
ADZUNA_APP_ID=
ADZUNA_APP_KEY=

# Reed.co.uk – UK jobs
# Register: https://www.reed.co.uk/developers/jobseeker
REED_API_KEY=

# USAJobs – US government positions
# Register: https://developer.usajobs.gov/APIRequest/Index
USAJOBS_API_KEY=
USAJOBS_EMAIL=

# Jooble – massive global aggregator (thousands of boards)
# Register: https://jooble.org/api/about
JOOBLE_API_KEY=

# SerpAPI – Google Jobs search (100 free searches/month)
# This searches Google's job aggregation which pulls from Indeed,
# LinkedIn, Glassdoor, ZipRecruiter and thousands more.
# Register: https://serpapi.com/
SERPAPI_KEY=

# Findwork.dev – developer / tech jobs
# Register: https://findwork.dev/developers/
FINDWORK_API_KEY=

# CareerJet – job aggregator (1000 calls/hour free)
# Register: https://www.careerjet.com/partners/api
CAREERJET_AFFID=

# JobData API – optional. Without key: ~10 requests/hour (testing). With key: no hourly limit.
# Register: https://jobdataapi.com/accounts/pricing/
JOBDATA_API_KEY=

# JobData: countries to include (ISO 3166-1 alpha-2). Comma-separated. Default: US,GB (US + UK). Empty = all countries.
JOBDATA_COUNTRIES=US,GB

# JobSpy: delay in seconds between each scrape (keyword/country) to reduce Google 429/CAPTCHA. Default 8.
# JOBSPY_DELAY_BETWEEN_REQUESTS=8

# LinkedIn (Direct): delay in seconds between pagination requests (default 5). Runs alongside JobSpy LinkedIn.
# LINKEDIN_DIRECT_DELAY=5

# LinkedIn (Direct): locations when user doesn't specify one. Comma-separated (default: US + UK).
# LINKEDIN_DIRECT_LOCATIONS=United States,United Kingdom

# LinkedIn (Direct) – optional browser mode: open real LinkedIn jobs page in a browser, scrape, then close.
# Lets you log in once (profile saved); then runs logged-in. Requires: pip install playwright && playwright install chromium
# LINKEDIN_DIRECT_USE_BROWSER=true
# LINKEDIN_DIRECT_BROWSER_HEADED=true
# LINKEDIN_DIRECT_BROWSER_PROFILE=logs/linkedin_browser_profile
# LINKEDIN_DIRECT_CARD_DELAY=1.0

# Optional: override Greenhouse company boards (comma-separated). If unset, 100+ default boards are used (tech, fintech, etc.)
# Add or replace: GREENHOUSE_BOARD_TOKENS=stripe,gitlab,github,yourcompany

# JobSpy: which sites to scrape. Comma-separated.
# Options: indeed, linkedin, glassdoor, zip_recruiter, google, bayt (Middle East), naukri (India), bdjobs (Bangladesh)
# Default omits google (often 429 rate limit). EU: omit zip_recruiter (403 GDPR).
JOBSPY_SITES=indeed,linkedin,glassdoor,zip_recruiter,bayt,naukri,bdjobs

# JobSpy / LinkedIn: countries to search (Indeed/Glassdoor are per-country). Comma-separated.
# Default: USA. For "everywhere" use several, e.g. USA,UK,Canada,Australia,Germany,France,India
# JobSpy accepts: usa, uk, canada, australia, germany, france, india, netherlands, ireland, brazil, mexico, spain, italy, japan, singapore, etc.
JOBSPY_COUNTRIES=USA

# ══════════════════════════════════════════════════════════════
# Application Settings
# ══════════════════════════════════════════════════════════════

# Flask secret key (change in production)
SECRET_KEY=change-me-to-a-random-secret-key

# Data directory (used for CSV exports)
DATA_DIR=data

# Log directory; errors/warnings are appended to logs/error_log.txt
# LOG_DIR=logs
# ERROR_LOG_FILE=logs/error_log.txt

# Maximum results per source per search (default 1000; UI slider allows 25–1000)
MAX_RESULTS_PER_SOURCE=1000

# Request timeout in seconds
REQUEST_TIMEOUT=30

# Rate limit delay between API calls (seconds)
RATE_LIMIT_DELAY=1.0

# ══════════════════════════════════════════════════════════════
# MySQL Database (XAMPP default: root with no password)
# ══════════════════════════════════════════════════════════════

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=job_search
